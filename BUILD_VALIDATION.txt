ExtraCare AI — Groq Migration Build Validation
==============================================

Provider migration completed on 2026-09-10.

Static checks performed in the build environment:
- Python source compilation: PASS
- JSON knowledge-base parsing: PASS
- Groq/Google provider reference scan: PASS after documented historical references were removed
- Secret hygiene scan logic updated to recognize Groq gsk_ keys
- .env remains excluded from the package; .env.example contains placeholders only
- Project keeps Streamlit + LangGraph + Tavily + Chroma + LangSmith architecture

Live checks NOT fabricated:
- No user GROQ_API_KEY, TAVILY_API_KEY, or LANGSMITH_API_KEY was embedded in this package.
- The build environment could not install dependencies because outbound PyPI DNS was unavailable.
- Therefore live Groq OCR/chat calls, first-run local embedding-model download, Tavily calls, browser execution, and remote LangSmith retention/share must be verified on the user's configured machine.

Run on the final machine:
  py scripts/project.py setup
  py scripts/project.py check
  py scripts/project.py live
  py scripts/project.py run

- Dependency-light Groq migration regression tests: 6 passed.
